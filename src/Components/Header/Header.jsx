import './Header.css'

const Header = ({}) => {
    return ( 
        <header>
            <span className="header-text">Social</span><span className="header-text feed">Feed</span>
        </header>
    );
}
 
export default Header;