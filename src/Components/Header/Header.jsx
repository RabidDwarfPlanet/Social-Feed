import './Header.css'

const Header = ({}) => {
    return ( 
        <header>
            <span className="headerText">Social</span><span className="headerText feed">Feed</span>
        </header>
    );
}
 
export default Header;